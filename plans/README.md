# Implementation Plans

Generated by the improve skill on 2026-06-26. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                               | Priority | Effort | Depends on | Status |
| ---- | ----------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | In-app "What's New" after an update | P2       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None. 001 is self-contained.

## Context for this batch

This is not an audit batch — it is a single feature plan requested directly by
the maintainer. We surveyed how open-source Electron apps surface release notes
(responsively-app fetches the installed version's GitHub Release at runtime and
gates on a seen-versions list; breach-protocol/amical render `electron-updater`'s
`releaseNotes`; nuclear/DIM bundle a changelog file). Plan 001 implements the
**fetch-at-runtime** approach because it keeps the GitHub Release body as the
single source of the friendly copy (no second file to maintain, no extra release
step) and works for the version the user is _currently running_ — which is the
moment the maintainer wants the notes shown (first launch after an update).

## Findings considered and rejected

- **Bundle a `whatsnew.json` in the app** (nuclear-style): rejected as the primary
  approach because it adds a per-release maintenance step and a packaged asset;
  the runtime-fetch approach reuses the GitHub Release notes the maintainer already
  writes. Revisit only if the repo's releases become private/unauthenticated (see
  STOP conditions in 001).
- **Render `electron-updater`'s pending-download `releaseNotes`** (breach-protocol-style):
  not rejected, but out of scope for 001 — that surfaces notes for an update that
  has _downloaded but not yet installed_, a different moment than this plan targets.
  Listed as a deferred follow-up in 001's maintenance notes.
